apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: quotes-vms-legacy
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
        - name: backend
          env: backend
          server: https://kubernetes.default.svc
        - name: frontend
          env: frontend
          server: https://kubernetes.default.svc
  template:
    metadata:
      name: "{{name}}-quotes-vms-legacy"
    spec:
      project: default
      source:
        repoURL: https://github.com/ignaciolago/gitops-demo.git
        targetRevision: quotes
        path: "applicationsets/quotes-vms-legacy/kustomize/{{env}}"
        kustomize:
          commonAnnotations:
            managed-cluster: "{{name}}"
      destination:
        server: "{{server}}"
        namespace: "{{name}}-quotes-vms-legacy"
      syncPolicy:
        syncOptions:
        - CreateNamespace=true
        automated:
          selfHeal: true
          prune: true
